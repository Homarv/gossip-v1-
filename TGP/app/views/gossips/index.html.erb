<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">    <title>Contact</title>
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%=gossips_path %>">The Gossip Project</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="<%=gossips_path %>">Home <span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link" href="<%=team_path %>">Team</a>
      <a class="nav-item nav-link" href="<%=contact_path %>">Contact</a>
    </div>
  </div>
</nav>

<div class="container text-center">
    <h1>Home</h1>
    <a class="nav-item nav-link" href="<%= new_gossip_path%>">Créer un nouveau potin </a>
    <p>Voici la liste de tous les potins</p>

    
</div>


<div class="container ">
<% @gossips.each do |gossip| %>
  <div class="container ">
   
      <div class="card-deck mb-3 text-center">
        <div class="card mb-4 box-shadow">
          <div class="card-header">
            <h4 class="my-0 font-weight-normal"><%= gossip.user.first_name %> <%= gossip.user.last_name %></h4>
          </div>
          <div class="card-body">
            
            <div class="list-unstyled mt-3 mb-4">
              <li> <%= gossip.title %></li>
            </div>
            <button type="button" class="btn btn-lg btn-block btn-dark"><%= link_to "Voir le potin", gossip_path(gossip.id) %> </button>
          </div>  
        </div>
    <%end%>

  </div>
</div>


</body>
</html>